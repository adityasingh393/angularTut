export type DropDownButtonListType = {
  title: string;
};

export type DropDownButtonForGraph = {
  title: string;
  value: string;
};

// export interface notificationType {
//   type: 'success' | 'error' | 'none' | 'info' | 'warning'|undefined;
// }
